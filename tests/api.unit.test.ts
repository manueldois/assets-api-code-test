import 'dotenv/config'
import '../index'
import { beforeEach, describe, expect, vi, it } from 'vitest';

describe('API Unit Tests', () => {
    it('works', async () => {
        expect(1).toBe(1)
    })
})